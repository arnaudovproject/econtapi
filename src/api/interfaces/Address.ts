export interface Address {
    id: number
}

export interface City {
    fullAddress: string,
    fullAddressEn: string,
    quarter: string,
    street: string,
    num: string,
    other: string
}

export interface Location {
    zip: string,
    hezid: string
}